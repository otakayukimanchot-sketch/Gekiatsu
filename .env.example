# Backend URL for Socket.io (Leave empty for local server in AI Studio)
VITE_BACKEND_URL=

# Frontend URL for CORS (Leave empty for local server in AI Studio)
VITE_FRONTEND_URL=

# SMTP Configuration for Suggestions (Optional)
# To enable real email sending, provide Gmail credentials (App Password recommended)
SMTP_USER=
SMTP_PASS=
